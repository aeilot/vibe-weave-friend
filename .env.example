# Database Configuration
# For Neon Serverless PostgreSQL (Recommended for production)
DATABASE_URL="postgresql://username:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require"
DIRECT_URL="postgresql://username:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require"

# OpenAI API Configuration (Optional - can be configured in UI)
VITE_OPENAI_API_KEY="sk-..."

# Supabase Configuration (Optional - if using Supabase features)
VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-key"

# Backend API URL (Optional - if using separate backend)
VITE_API_URL="http://localhost:3000"
